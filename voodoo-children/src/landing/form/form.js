import React from 'react';
import './form.css'

const Form = () => {
    return (
        <div className="form" style={{height: `${window.innerHeight}px`}}>
            <h1>Form / CTA</h1>
        </div>
    );
};

export default Form;